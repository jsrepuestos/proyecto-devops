name: CI-CD Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to GCP VM via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd ~/proyecto-devops

            # eliminar contenedor completamente (FORZADO)
            docker rm -f mi-web || true

            # construir imagen
            docker build -t mi-web .

            # ejecutar contenedor
            docker run -d -p 8080:80 --name mi-web --restart unless-stopped mi-web
